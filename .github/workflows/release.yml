name: Build & Release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Optional tag to attach artifacts to (must already exist as a release). Leave blank for artifact-only run."
        required: false
        default: ""
      platforms:
        description: "Which platforms to build (comma-separated subset of: macos,windows,linux,android). Default: all."
        required: false
        default: "all"

jobs:
  setup:
    name: Resolve build matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.gen.outputs.matrix }}
    steps:
      - id: gen
        shell: bash
        env:
          PLATFORMS: ${{ github.event_name == 'workflow_dispatch' && inputs.platforms || 'all' }}
        run: |
          all='[
            {"name":"macos-universal","runner":"macos-14","platform":"macos","shader_platform":"macos","arch":"universal"},
            {"name":"windows-x86_64","runner":"windows-2022","platform":"windows","shader_platform":"windows","arch":"x86_64"},
            {"name":"linux-x86_64","runner":"ubuntu-22.04","platform":"linux","shader_platform":"linuxbsd","arch":"x86_64"},
            {"name":"android-arm64","runner":"ubuntu-22.04","platform":"android","shader_platform":"linuxbsd","arch":"arm64"}
          ]'
          if [ "$PLATFORMS" = "all" ]; then
            filtered="$all"
          else
            filtered=$(echo "$all" | python3 -c "import json,sys,os; sel=set(s.strip() for s in os.environ['PLATFORMS'].split(',') if s.strip()); print(json.dumps([c for c in json.load(sys.stdin) if c['platform'] in sel]))")
          fi
          echo "matrix=$(echo "$filtered" | tr -d '\n' | tr -s ' ')" >> "$GITHUB_OUTPUT"

  build:
    name: ${{ matrix.config.name }} (${{ matrix.target }})
    needs: setup
    runs-on: ${{ matrix.config.runner }}
    strategy:
      fail-fast: false
      matrix:
        target: [template_debug, template_release]
        config: ${{ fromJSON(needs.setup.outputs.matrix) }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install SCons
        run: python -m pip install --upgrade pip scons ply

      # Linux deps: OpenGL / X11 headers + glslang/spirv-tools for Rive shader build.
      - name: Install Linux build deps
        if: matrix.config.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libgl1-mesa-dev libx11-dev libxrandr-dev libxcursor-dev libxinerama-dev libxi-dev glslang-tools spirv-tools

      # Android: install shader tools.
      - name: Install Android build deps
        if: matrix.config.platform == 'android'
        run: |
          sudo apt-get update
          sudo apt-get install -y glslang-tools spirv-tools

      # Verify the pre-installed NDK toolchain is usable.
      - name: Verify Android NDK toolchain
        if: matrix.config.platform == 'android'
        shell: bash
        run: |
          echo "ANDROID_NDK_ROOT=${ANDROID_NDK_ROOT}"
          ls "${ANDROID_NDK_ROOT}/toolchains/llvm/prebuilt/linux-x86_64/bin/" | head -5

      # Fetch modern Vulkan headers (NDK sysroot lacks VK_ARM_format_pack added in 1.4.318)
      # and VulkanMemoryAllocator. Expose them as a synthetic VULKAN_SDK so SConscript.rive
      # prepends them to CPPPATH, overriding the NDK's stale vulkan_core.h.
      - name: Fetch Vulkan headers and VMA for Android
        if: matrix.config.platform == 'android'
        shell: bash
        run: |
          ANDROID_VULKAN_SDK="$RUNNER_TEMP/android-vulkan-sdk"
          mkdir -p "${ANDROID_VULKAN_SDK}/include"
          # Vulkan-Headers 1.4.321 (adds VK_ARM_format_pack enums missing from NDK sysroot)
          curl -fsSL "https://github.com/KhronosGroup/Vulkan-Headers/archive/refs/tags/v1.4.321.tar.gz" \
            -o "$RUNNER_TEMP/vulkan-headers.tar.gz"
          tar -xzf "$RUNNER_TEMP/vulkan-headers.tar.gz" -C "$RUNNER_TEMP"
          cp -r "$RUNNER_TEMP/Vulkan-Headers-1.4.321/include/"* "${ANDROID_VULKAN_SDK}/include/"
          # VulkanMemoryAllocator (header-only)
          curl -fsSL -o "${ANDROID_VULKAN_SDK}/include/vk_mem_alloc.h" \
            "https://raw.githubusercontent.com/GPUOpen-LibrariesAndSDKs/VulkanMemoryAllocator/v3.1.0/include/vk_mem_alloc.h"
          mkdir -p "${ANDROID_VULKAN_SDK}/include/vma"
          cp "${ANDROID_VULKAN_SDK}/include/vk_mem_alloc.h" "${ANDROID_VULKAN_SDK}/include/vma/"
          echo "VULKAN_SDK=${ANDROID_VULKAN_SDK}" >> "$GITHUB_ENV"
          echo "Android Vulkan SDK ready:"
          ls "${ANDROID_VULKAN_SDK}/include/"

      # Windows: enable MSVC + ensure fxc.exe (Windows SDK) is on PATH for shader build.
      - name: Setup MSVC
        if: matrix.config.platform == 'windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      # Vulkan SDK on Windows/Linux (Rive needs Vulkan headers >= 1.4.318 for VK_ARM_format_pack).
      - name: Install Vulkan SDK
        if: matrix.config.platform == 'windows' || matrix.config.platform == 'linux'
        uses: jakoch/install-vulkan-sdk-action@v1
        with:
          vulkan_version: 1.4.321.1
          install_runtime: false
          cache: true
          stripdown: false

      - name: Verify Vulkan SDK headers
        if: matrix.config.platform == 'windows' || matrix.config.platform == 'linux'
        shell: bash
        run: |
          echo "VULKAN_SDK=$VULKAN_SDK"
          ls "$VULKAN_SDK/Include/vulkan/vulkan.h" 2>/dev/null || ls "$VULKAN_SDK/include/vulkan/vulkan.h"

      # Vulkan Memory Allocator (header-only) — Rive's vulkan backend includes vk_mem_alloc.h.
      - name: Fetch VulkanMemoryAllocator
        if: matrix.config.platform == 'windows' || matrix.config.platform == 'linux'
        shell: bash
        run: |
          mkdir -p "$RUNNER_TEMP/vma/include/vma"
          curl -fsSL -o "$RUNNER_TEMP/vma/include/vk_mem_alloc.h" \
            https://raw.githubusercontent.com/GPUOpen-LibrariesAndSDKs/VulkanMemoryAllocator/v3.1.0/include/vk_mem_alloc.h
          cp "$RUNNER_TEMP/vma/include/vk_mem_alloc.h" "$RUNNER_TEMP/vma/include/vma/vk_mem_alloc.h"
          # Place the header inside the SDK's include directory so SConscript.rive picks it up.
          # Windows uses 'Include', Linux uses 'include'.
          for inc in "$VULKAN_SDK/Include" "$VULKAN_SDK/include"; do
            if [ -d "$inc" ]; then
              mkdir -p "$inc/vma"
              cp "$RUNNER_TEMP/vma/include/vk_mem_alloc.h" "$inc/vk_mem_alloc.h"
              cp "$RUNNER_TEMP/vma/include/vk_mem_alloc.h" "$inc/vma/vk_mem_alloc.h"
              ls "$inc/vk_mem_alloc.h"
            fi
          done

      # DirectX-Headers (provides d3dx12.h) for the Rive D3D12 renderer.
      - name: Fetch DirectX-Headers (Windows)
        if: matrix.config.platform == 'windows'
        shell: bash
        run: |
          git clone --depth 1 --branch v1.614.0 https://github.com/microsoft/DirectX-Headers.git "$RUNNER_TEMP/DirectX-Headers"
          echo "DIRECTX_HEADERS_PATH=$RUNNER_TEMP/DirectX-Headers" >> "$GITHUB_ENV"

      - name: Generate Rive shaders
        shell: bash
        run: |
          python3 scripts/build_shaders.py \
            third-party/rive-runtime/renderer/src/shaders \
            third-party/rive-runtime/renderer/include/generated/shaders \
            ${{ matrix.config.shader_platform }}

      - name: Build
        shell: bash
        run: |
          # For Android: detect the installed NDK version dynamically and pass it
          # to scons so godot-cpp doesn't use its hardcoded default (28.1.x).
          if [ "${{ matrix.config.platform }}" = "android" ]; then
            NDK_VER=$(ls "${ANDROID_SDK_ROOT}/ndk/" | sort -V | tail -1)
            echo "Using NDK version: ${NDK_VER}"
            scons platform=${{ matrix.config.platform }} target=${{ matrix.target }} arch=${{ matrix.config.arch }} ndk_version="${NDK_VER}" -j$(getconf _NPROCESSORS_ONLN 2>/dev/null || echo 4)
          else
            scons platform=${{ matrix.config.platform }} target=${{ matrix.target }} arch=${{ matrix.config.arch }} -j$(getconf _NPROCESSORS_ONLN 2>/dev/null || echo 4)
          fi

      - name: Stage artifacts
        shell: bash
        run: |
          mkdir -p staged/addons/godot-rive/bin
          cp -R project/addons/godot-rive/godot-rive.gdextension staged/addons/godot-rive/ || true
          cp -R project/addons/godot-rive/godot-rive.gdextension.uid staged/addons/godot-rive/ 2>/dev/null || true
          # Copy whatever the build produced for this platform/target.
          cp -R project/addons/godot-rive/bin/* staged/addons/godot-rive/bin/ || true
          ls -R staged

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: godot-rive-${{ matrix.config.name }}-${{ matrix.target }}
          path: staged/**
          if-no-files-found: error
          retention-days: 14

  package:
    name: Package & attach to release
    needs: build
    runs-on: ubuntu-22.04
    if: always() && needs.build.result == 'success'
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Merge artifacts into a single addon tree
        run: |
          set -e
          mkdir -p package/addons/godot-rive/bin
          # Each artifact already has the addon layout; merge them.
          for d in dist/*/addons/godot-rive; do
            cp -R "$d/." package/addons/godot-rive/
          done
          ls -R package
          (cd package && zip -r ../godot-rive-${{ github.ref_name }}.zip addons)
          ls -lh godot-rive-*.zip

      - name: Upload merged zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: godot-rive-addon-zip
          path: godot-rive-*.zip
          retention-days: 30

      - name: Attach to GitHub Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: godot-rive-*.zip
